- name: Git clone Prezto
  when: username|default(None) != None
  git:
    repo: "{{ prezto_repo_url }}"
    dest: "/home/{{ username }}/.zprezto"
    accept_hostkey: yes
    clone: yes
    update: yes
